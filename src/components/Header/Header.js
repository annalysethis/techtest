import React from "react";
import { HeaderMenu, Headline } from "./Header.styled";

const Header = () => {
  return (
    <HeaderMenu>
      <Headline>Hår</Headline>
    </HeaderMenu>
  );
};

export default Header;
